FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir requests reportlab && \
    apt-get update && apt-get install -y --no-install-recommends fonts-dejavu-core && \
    rm -rf /var/lib/apt/lists/*

COPY stars_bot.py .

CMD ["python3", "stars_bot.py"]
